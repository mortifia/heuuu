to test run :
```
//non blockant et pas fini (pas testé ur autre choses que ubuntu)
//c'est pour installer la db
./install.sh

// install des lib
npm install

//pour le lancé simplement
npm run dev
```